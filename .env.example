# Basic Auth (leave empty to skip auth in development)
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Username for wallpaper URL (e.g., /api/myname)
NEXT_PUBLIC_APP_USERNAME=user

# Data directory for config persistence (mount as persistent volume in Coolify)
DATA_DIR=./data
